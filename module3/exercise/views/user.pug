extends layouts/main-layout.pug

block content
    main
        h1 Users
            if users.length > 0
                ul
                    each user in users
                        li.user #{user.username}
            else 
                h2 No users